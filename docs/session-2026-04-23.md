# Development Session — 2026-04-23

## Objective

Implement a proper Gitflow-based PR and release workflow:
- CI runs on PRs to `develop` only (no more direct pushes to `main`)
- CD triggers on `release/*` branches, auto-merges to `main` and tags after a successful deploy
- PR template, branch protection, and CLAUDE.md branching rules

---

## What Was Done

### 1. CI workflow — trigger changed to PRs only

**Before:** CI ran on every push to `main` AND on PRs.

**After:** CI runs only on `pull_request` events targeting `develop`.

```yaml
on:
  pull_request:
    branches: ["develop"]
    types: [opened, synchronize, reopened]
```

Jobs unchanged: flake8 → black --check → mypy → pytest tests/unit/

File: `.github/workflows/ci.yaml`

---

### 2. CD workflow — release/* trigger + auto merge-to-main

**Before:** Triggered on `push: branches: ["main"]`. Four jobs: test, build-push, deploy-production, deploy-staging.

**After:** Triggered on `push: branches: ["release/*"]`. Four jobs: test, build-push, deploy-production, merge-to-main.

Key changes:
- Trigger changed from `main` to `release/*`
- `deploy-production` environment gate (manual approval) kept
- New job `merge-to-main` runs after successful deploy:
  - Extracts version from branch name (`release/v1.1.0` → `v1.1.0`)
  - Merges release branch into `main` with `--no-ff`
  - Creates version tag and pushes

```yaml
- name: Extract version from branch name
  id: version
  run: |
    BRANCH="${GITHUB_REF#refs/heads/}"
    VERSION="${BRANCH#release/}"
    echo "branch=$BRANCH" >> $GITHUB_OUTPUT
    echo "version=$VERSION" >> $GITHUB_OUTPUT

- name: Merge release branch into main
  run: |
    git config user.name "github-actions[bot]"
    git config user.email "github-actions[bot]@users.noreply.github.com"
    git checkout main
    git merge --no-ff origin/${{ steps.version.outputs.branch }} \
      -m "chore: merge ${{ steps.version.outputs.branch }} into main"
    git push origin main

- name: Create version tag on main
  run: |
    git tag ${{ steps.version.outputs.version }}
    git push origin ${{ steps.version.outputs.version }}
```

Staging job removed (staging branch is no longer part of the workflow).

File: `.github/workflows/cd.yaml`

---

### 3. PR description template

Created `.github/pull_request_template.md` — shown automatically on every new PR in GitHub.

Sections: Summary, Type of change (checklist), Testing, Checklist (lint/format/type/test/secrets/CLAUDE.md/openapi).

---

### 4. CLAUDE.md — branching strategy section added

Added full **Branching strategy** section documenting:
- Branch naming table (main, develop, feature/*, bugfix/*, release/vX.Y.Z, hotfix/*)
- Step-by-step PR workflow
- Hotfix workflow
- Merge strategy (squash for PRs, --no-ff for releases)
- Branch protection summary

Also fixed two stale references left from the Apigee session:
- `api_key_auth.py` → `caller_identity_auth.py` in the layout comment
- `ApiKeyAuthMiddleware` → `CallerIdentityMiddleware` in the request flow and OWASP table

---

### 5. Branch protection applied

Repository was made public (required for free GitHub plan).

| Branch | Rules |
|--------|-------|
| `develop` | PR required · 1 approval required · CI check `Lint, type-check, and test` must pass · no force-push · no deletion |
| `main` | No force-push · no deletion (CD pipeline merges directly after deploy) |

Applied via `gh api repos/.../branches/.../protection` with `--method PUT`.

---

### 6. Full workflow tested end-to-end

**Step 1 — develop sync:**
`develop` was 17 commits behind `main` (all prior work went directly to main). Fast-forwarded develop to match main one-time.

**Step 2 — feature branch → PR:**
```bash
git checkout -b feature/pr-workflow develop
# commit ci.yaml, cd.yaml, CLAUDE.md, pull_request_template.md
git push origin feature/pr-workflow
gh pr create --base develop --head feature/pr-workflow
```
PR #1 opened. CI ran (`Lint, type-check, and test`). Approved and squash-merged into `develop`.

**Step 3 — release branch → CD:**
```bash
git checkout develop && git pull
git checkout -b release/v1.1.0
git push origin release/v1.1.0
```
CD pipeline triggered: test → build → deploy production (manual approval gate) → merge `release/v1.1.0` → `main` → tag `v1.1.0`.

---

## Conflict Behaviour — Important Note

If `merge-to-main` encounters a conflict, the job **fails** but the GKE deploy has already succeeded. In that case, merge and tag manually:

```bash
git checkout main && git pull
git checkout release/vX.Y.Z
git merge main        # resolve conflicts
git push origin release/vX.Y.Z

git checkout main
git merge --no-ff release/vX.Y.Z
git push origin main
git tag vX.Y.Z && git push origin vX.Y.Z
```

**Prevention:** Always back-merge hotfixes into `develop` immediately. Conflicts on `main` only happen when a hotfix was applied to `main` but not back-ported.

---

## Additional Work — Same Session

### Infrastructure start/stop scripts

Added on-demand start/stop to avoid paying for Cloud SQL compute 24/7.

| File | Purpose |
|------|---------|
| `infra/config.env` | Single source of truth — region, project, cluster, SQL instance, namespace |
| `infra/variables.tf` | Added `sql_active` bool (default `true`) |
| `infra/cloudsql.tf` | Added `activation_policy = var.sql_active ? "ALWAYS" : "NEVER"` |
| `start.sh` | Starts SQL → waits for RUNNABLE → scales pods to 3 |
| `stop.sh` | Scales pods to 0 → waits for termination → stops SQL |
| `docs/daily_activity.md` | Step-by-step daily runbook: start, dev workflow, deploy, hotfix, stop |

To change region or project: edit `infra/config.env` only. Both scripts derive all values from it.
`TF_VAR_db_password` must be exported in the shell (not stored in any file).

### Architecture design document

Created `docs/architecture.md` covering:

- Full system diagram (internet → LB → Apigee → ILB → GKE → Cloud SQL)
- All 13 GCP components with type, role, and key config
- Step-by-step OAuth token flow and API request flow
- Middleware chain detail (CorrelationId → SecurityHeaders → CallerIdentity)
- Timeout budget (nested diagram)
- Security controls at each layer
- Resilience patterns (circuit breaker, HPA, rollback)
- Future state: GKE Gateway (pending real domain)

### Apigee base path changed: `/v1/customer-service` → `/v1/piapp`

Old path was service-specific. A platform-neutral path works for any future service
(e.g. order-service) added under the same gateway.

Files updated:

- `apigee/bundle/apiproxy/proxies/default.xml` — `<BasePath>/v1/piapp</BasePath>`
- `apigee/proxy-endpoint.xml` — same
- `apigee/customer-service-pi-proxy.zip` — regenerated bundle
- `apigee/README.md` — curl examples updated
- `docs/architecture.md` — all URL references updated

Deployed to Apigee (revision 2, eval env):

```bash
# Import
curl -X POST ".../apis?action=import&name=customer-service-pi" -F "file=@bundle.zip"
# Deploy with override (replaces revision 1)
curl -X POST ".../revisions/2/deployments?override=true"
```

Verified:

- Old path `/v1/customer-service/*` → HTTP 404 ✓
- New token endpoint `/v1/piapp/oauth/token` → token issued ✓
- New API path `/v1/piapp/health` with Bearer token → `{"status": "ok"}` ✓

New base URL: `https://api.34-107-235-25.nip.io/v1/piapp`

---

## What Was NOT Done (Deferred)

| Item | Notes |
|---|---|
| Workload Identity Federation | Requires project owner — WIF pool creation blocked by SA permissions |
| Gateway + HTTPRoute applied | Using nip.io; apply when real domain available |
| GCE quota increase | For multi-zone pod spreading |
| Redis client | Pattern in `.claude/skills/resilience_SKILL.md` |
| Algolia client | Pattern in `.claude/skills/resilience_SKILL.md` |
| Integration tests | Require `docker-compose.yml` |

---

## Updated: New Service Setup Checklist — GitHub section

The GitHub checklist in session-2026-04-21.md should now reflect the branching strategy:

- [ ] `develop` branch created and protected (PR + 1 approval + CI check)
- [ ] `main` branch protected (no force-push, no deletion)
- [ ] **GitHub environment: `production`** — created with manual approval gate
- [ ] First feature branch + PR flow tested before any release

---

## Final Workflow Summary

```text
feature/* or bugfix/*
  └── PR → develop (CI required + 1 approval, squash merge)

develop
  └── git checkout -b release/vX.Y.Z develop && git push
        └── CD: test → build+push → deploy production (manual approval)
                 → merge release/vX.Y.Z → main (--no-ff)
                 → tag vX.Y.Z

hotfix/*  (urgent prod fix)
  └── branch from main → PR → main (regular merge)
  └── also PR → develop (back-merge, keep in sync)
```
